# RecipeAPI
This API is designed to contain recipes that can be filtered on eg. ingredients and cooking time.

## Running the API
First copy `.env.example` and rename it to `.env`, edit the environment variables.

Then build and run the application using docker compose

```bash
docker compose up --build
```